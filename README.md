# Web Server

This is the web server application for the IGNIET project.

## Getting Started

### Using Docker

Build the Docker image:
```bash
docker build -t igniet-web-server .
```

Run the container:
```bash
docker run -p 8000:8000 igniet-web-server
```
